import type { NarrationCue, PracticeGeometryOutcome } from "./interaction";

// Re-export narrative/practice bits used by stepMeta (keeps imports tidy)
export type { NarrationCue, PracticeGeometryOutcome };
